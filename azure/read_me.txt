The __init__ Python script in the TimerTrigger1 folder fetches historical stock data from the Alpha Vantage API
and stores it in a Azure SQL database using the to_sql script.

The script is designed to be triggered by an Azure Function on a schedule or run manually from the command line